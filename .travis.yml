dist: trusty
language: cpp

matrix:
  include:
    - os: linux
      env:
        - MATRIX_EVAL="CC=clang-5.0 && CXX=clang++-5.0"
    - os: linux
      addons:
        apt:
          sources:
            - ubuntu-toolchain-r-test
          packages:
            - g++-7
      env:
        - MATRIX_EVAL="CC=gcc-7 && CXX=g++-7"

before_install:
  - eval "${MATRIX_EVAL}"
  - ls /usr/local
  - ls /usr/local/clang-5.0.0
  - ls /usr/local/clang-5.0.0/bin

script:
  - mkdir build
  - cd build
  - cmake ../libft0cc
  - cmake --build . -- -j4
  - ctest -V
